{
  "name": "amigo-secreto-navideno-functions",
  "version": "1.0.0",
  "description": "Dependencies for Netlify Functions",
  "main": "functions/send-email.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "moment": "^2.30.1",
    "@sendgrid/mail": "^7.7.0" 
  }
}